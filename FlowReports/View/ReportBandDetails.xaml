<Window x:Class="FlowReports.View.ReportBandDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:editoritems="clr-namespace:FlowReports.ViewModel.EditorItems"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:FlowReports.Properties"
        xmlns:toolsUI="clr-namespace:ES.Tools.UI;assembly=ES.Tools"
        xmlns:viewmodel="clr-namespace:FlowReports.ViewModel" xmlns:converters="clr-namespace:ES.Tools.Converters;assembly=ES.Tools"
        Title="{x:Static properties:Resources.Details}"
        Width="600"
        Height="150"
        d:DataContext="{d:DesignInstance Type=editoritems:ReportBandViewModel}"
        mc:Ignorable="d">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <toolsUI:SharedResourceDictionary Source="/FlowReports;component/View/Styles.xaml" />
      </ResourceDictionary.MergedDictionaries>

      <converters:NegationConverter x:Key="NegationConverter" /> 
    </ResourceDictionary>
  </Window.Resources>

  <Grid Margin="5">
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Row="1" Style="{StaticResource LabelTextBlockStyle}" Text="{x:Static properties:Resources.DataSource}" />
      <TextBox Grid.Row="1"
               Grid.Column="1"
               Style="{StaticResource EditTextBoxStyle}"
               Text="{Binding DataSource}" />
      <TextBlock Grid.Row="2" Style="{StaticResource LabelTextBlockStyle}" Text="{x:Static properties:Resources.Height}" />
      <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
        <TextBox Width="150" Style="{StaticResource DoubleTextBoxStyle}" Text="{Binding Height}" IsEnabled="{Binding HeightAuto, Converter={StaticResource NegationConverter} }" />
        <CheckBox Content="{x:Static properties:Resources.Auto}" IsChecked="{Binding HeightAuto}" Style="{StaticResource EditCheckBoxStyle}" />
      </StackPanel>
    </Grid>
    <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal">
      <Button Click="OKButton_Click" Style="{StaticResource OKButton}" />
      <Button Click="CancelButton_Click" Style="{StaticResource CancelButton}" />
    </StackPanel>
  </Grid>
</Window>
