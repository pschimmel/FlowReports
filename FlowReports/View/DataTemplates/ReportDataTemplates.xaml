<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:converters="clr-namespace:FlowReports.View.Converters"
                    xmlns:toolsUI="clr-namespace:ES.Tools.UI;assembly=ES.Tools" xmlns:vm="clr-namespace:FlowReports.ViewModel.ReportItems">
  <ResourceDictionary.MergedDictionaries>
    <toolsUI:SharedResourceDictionary Source="pack://application:,,,/FlowReports;component/View/Brushes.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <converters:ObjectToImageSourceConverter x:Key="ImageConverter" />

  <Style x:Key="TextItemTextBlock" TargetType="{x:Type TextBlock}">
    <Setter Property="Margin" Value="0" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Background" Value="Transparent" />
  </Style>

  <DataTemplate DataType="{x:Type vm:TextItemViewModel}">
    <Border Canvas.Left="{Binding Left}"
            Canvas.Top="{Binding Top}"
            Width="{Binding Width}"
            Height="{Binding Height}"
            Background="Transparent" BorderBrush="Transparent" BorderThickness="1"
            Focusable="True">
      <TextBlock Text="{Binding Text}">
        <TextBlock.Style>
          <Style BasedOn="{StaticResource TextItemTextBlock}" TargetType="{x:Type TextBlock}">
            <Style.Triggers>
              <DataTrigger Binding="{Binding Bold}" Value="true">
                <Setter Property="FontWeight" Value="Bold" />
              </DataTrigger>
              <DataTrigger Binding="{Binding Italic}" Value="true">
                <Setter Property="FontStyle" Value="Italic" />
              </DataTrigger>
              <DataTrigger Binding="{Binding Underline}" Value="true">
                <Setter Property="TextDecorations" Value="Underline" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </TextBlock.Style>
      </TextBlock>
    </Border>
  </DataTemplate>

  <DataTemplate DataType="{x:Type vm:BooleanItemViewModel}">
    <Border Canvas.Left="{Binding Left}"
            Canvas.Top="{Binding Top}"
            Width="{Binding Width}"
            Height="{Binding Height}"
            Background="Transparent" BorderBrush="Transparent" BorderThickness="1"
            Focusable="True">
      <Path HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Data="M 0 0 L 8 8 M 0 8 L 8 0"
            Stretch="Fill"
            Stroke="{StaticResource General.Foreground}"
            StrokeEndLineCap="Round" StrokeStartLineCap="Round" StrokeThickness="2" />
    </Border>
  </DataTemplate>

  <DataTemplate DataType="{x:Type vm:ImageItemViewModel}">
    <Border Canvas.Left="{Binding Left}"
            Canvas.Top="{Binding Top}"
            Width="{Binding Width}"
            Height="{Binding Height}"
            Background="Transparent" BorderBrush="Transparent" BorderThickness="1"
            Focusable="True">
      <Image HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
             Source="{Binding ImageSource, Converter={StaticResource ImageConverter}}"
             Stretch="Uniform" />
    </Border>
  </DataTemplate>

</ResourceDictionary>